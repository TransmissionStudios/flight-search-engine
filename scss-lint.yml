exclude: sass/_normalize.scss

linters:
  SelectorFormat:
    enabled: false

  PropertySortOrder:
    order:
      - display
      - position
      - top
      - left
      - right
      - bottom
      - float
      - width
      - height
      - margin
      - margin-top
      - margin-left
      - margin-right
      - margin-bottom
      - padding
      - padding-top
      - padding-left
      - padding-right
      - padding-bottom
      - border
      - border-radius
      - background
      - content
      - counter-increment
      - font
      - font-weight
      - colour

  PropertyUnits:
    global: ['em', 'rem', '%'] # Allow relative units globally
    properties:
      border: ['px'] # Pixels only allowed for border
      line-height: [] # No units allowed
